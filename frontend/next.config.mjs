/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['team-track-avatars.s3.eu-north-1.amazonaws.com', 'lh3.googleusercontent.com']
  }
};

export default nextConfig;
